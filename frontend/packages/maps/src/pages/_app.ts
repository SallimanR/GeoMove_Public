import { createApp } from 'vue';
import type { App } from 'vue';
// import { $drivers } from '@stores/drivers';

// const app = createApp({});
export default (app: App) => {
	// app.use(devtools, { $drivers });
	// app.use(clientRouter)
}
