version: "2"

overrides:
  go:
    overrides:
      - db_type: "pg_catalog.timestamp"
        nullable: true
        engine: "postgresql"
        go_type:
          import: "time"
          type: "Time"
          pointer: true
      - db_type: "pg_catalog.timestamp"
        nullable: false
        engine: "postgresql"
        go_type:
          import: "time"
          type: "Time"
      - db_type: "pg_catalog.time"
        nullable: true
        engine: "postgresql"
        go_type:
          import: "time"
          type: "Time"
          pointer: true
      - db_type: "pg_catalog.time"
        nullable: false
        engine: "postgresql"
        go_type:
          import: "time"
          type: "Time"

sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./internal/auth/sql"
    gen:
      go:
        package: "sqlc"
        out: "./internal/auth/sqlc"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true

  - engine: "postgresql"
    schema: "./migrations"
    queries: "./internal/domains/driver/infrastructure/postgres/sql"
    gen:
      go:
        package: "sqlc"
        out: "./internal/domains/driver/infrastructure/postgres/sqlc"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true

  - engine: "postgresql"
    schema: "./migrations"
    queries: "./internal/domains/geolocation/infrastructure/postgres/sql"
    gen:
      go:
        package: "sqlc"
        out: "./internal/domains/geolocation/infrastructure/postgres/sqlc"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true

  - engine: "postgresql"
    schema: "./migrations"
    queries: "./internal/notification/sql"
    gen:
      go:
        package: "sqlc"
        out: "./internal/notification/sqlc"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true
